VITE_SANITY_PROJECT_ID=al7hyz6y
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
VITE_SANITY_TOKEN=YOUR_TOKEN_HERE

# ── Supabase (browser) ────────────────────────────────────────
VITE_SUPABASE_URL=https://gqccsklytrbihizglwba.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# ── Supabase (server only — NEVER prefix these with VITE_) ────
# VITE_ vars are bundled into the browser build. The service role key
# bypasses Row Level Security, so it must stay server-side.
SUPABASE_URL=https://gqccsklytrbihizglwba.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

# ── Email (Resend) ────────────────────────────────────────────
RESEND_API_KEY=YOUR_RESEND_API_KEY_HERE
COUPLE_EMAIL=your-email@example.com
SANITY_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET_HERE

# Sender for mass mail. Must be on a domain verified in Resend —
# onboarding@resend.dev can only deliver to your own account address.
MAIL_FROM=Semilore & Adeoluwa <hello@yourdomain.com>
MAIL_REPLY_TO=your-email@example.com

# Invitation attached to every campaign / preview email (PNG or PDF URL)
MAIL_ATTACHMENT_URL=https://res.cloudinary.com/dgnm4sny4/image/upload/v1785861240/SATURDAY_mye3v3.png
MAIL_ATTACHMENT_FILENAME=Wedding-Invitation.png

# ── Mass mail (RSVP broadcast) ────────────────────────────────
# Protects /api/campaigns/*. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_TOKEN=GENERATE_A_LONG_RANDOM_STRING
# Signs unsubscribe links. Generate the same way. Changing it invalidates
# every unsubscribe link already sent.
UNSUBSCRIBE_SECRET=GENERATE_A_LONG_RANDOM_STRING
# Used to build unsubscribe URLs and the "back to website" link.
PUBLIC_SITE_URL=https://your-wedding-site.vercel.app

# Optional tuning (defaults shown)
MAIL_BATCH_SIZE=25
MAIL_BATCH_DELAY_MS=600
MAIL_MAX_RUN_MS=240000
MAIL_MAX_ATTEMPTS=3
COUPLE_NAMES=Semilore & Adeoluwa
WEDDING_DATE=August 15, 2026

# Set this in Vercel to let the daily cron authenticate itself.
CRON_SECRET=GENERATE_A_LONG_RANDOM_STRING

# Only needed locally by scripts/mailer.mjs if it should target production.
# MAILER_BASE_URL=https://your-wedding-site.vercel.app
